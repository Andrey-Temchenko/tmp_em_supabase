{
  "name": "template_em_supabase",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm start",
    "format": "prettier \"(server|client)/src/**/*.{js,json,css}\" --write --config .prettierrc"
  }
}
